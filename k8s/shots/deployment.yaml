apiVersion: v1
kind: Service
metadata:
  labels:
    app: shot-wayl-one
  name: shot-wayl-one
  namespace: shot
spec:
  ports:
    - name: "5000"
      port: 5000
      targetPort: 5000
  selector:
    app: shot-wayl-one
---
apiVersion: v1
kind: Namespace
metadata:
  name: shot
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: shots-redis
  name: shots-redis
  namespace: shot
spec:
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  selector:
    app: shots-redis
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: shots-redis
  name: shots-redis
  namespace: shot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shots-redis
  template:
    metadata:
      labels:
        app: shots-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
              protocol: TCP
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: shots-redis-ingress
  namespace: shot
spec:
  podSelector:
    matchLabels:
      app: shots-redis
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
      ports:
        - protocol: TCP
          port: 6379
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: shot-wayl-one
  name: shot-wayl-one
  namespace: shot
spec:
  replicas: 4
  selector:
    matchLabels:
      io.kompose.service: shot-wayl-one
  template:
    metadata:
      annotations:
        diun.enable: "true"
      labels:
        app: shot-wayl-one
        io.kompose.service: shot-wayl-one
    spec:
      containers:
        - image: registry.wayl.one/shot-scraper-api:0.0.42
          command:
            - "/bin/bash"
            - "-c"
          args:
            - "uvicorn shot_scraper_api.api.app:app --host 0.0.0.0 --port 5000 --workers 1"
          name: shot-wayl-one
          ports:
            - containerPort: 5000
              protocol: TCP
          resources:
            requests:
              cpu: "100m"
              memory: "300Mi"
            limits:
              cpu: "1000m"
              memory: "4Gi"
          env:
            - name: ENV
              value: "prod"
            - name: CACHE_DIR
              value: "/cache"
            - name: QUEUE_BACKEND
              value: "redis"
            - name: QUEUE_PROCESSOR_ENABLED
              value: "false"
            - name: QUEUE_PROCESSOR_CONCURRENCY
              value: "4"
            - name: RENDER_CONCURRENCY
              value: "4"
            - name: POSTPROCESS_CONCURRENCY
              value: "2"
            - name: REDIS_URL
              value: redis://shots-redis.shot.svc.cluster.local:6379/0
            - name: AWS_ENDPOINT_URL
              value: http://minio.minio.svc.cluster.local:9000
          envFrom:
            - secretRef:
                name: shot-minio-secret
            - secretRef:
                name: shot-secret
          volumeMounts:
            - mountPath: /cache
              name: cache
      imagePullSecrets:
        - name: cluster-regcred
      restartPolicy: Always
      volumes:
        - name: cache
          persistentVolumeClaim:
            claimName: cache
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: shot-wayl-one-worker
  name: shot-wayl-one-worker
  namespace: shot
spec:
  replicas: 2
  selector:
    matchLabels:
      app: shot-wayl-one-worker
  template:
    metadata:
      labels:
        app: shot-wayl-one-worker
    spec:
      containers:
        - image: registry.wayl.one/shot-scraper-api:0.0.42
          command:
            - "/bin/bash"
            - "-c"
          args:
            - "python -m shot_scraper_api.worker"
          name: shot-wayl-one-worker
          livenessProbe:
            exec:
              command:
                - python
                - -m
                - shot_scraper_api.worker_health
                - --queue-stall
            initialDelaySeconds: 45
            periodSeconds: 15
            failureThreshold: 3
            timeoutSeconds: 5
          readinessProbe:
            exec:
              command:
                - python
                - -m
                - shot_scraper_api.worker_health
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          startupProbe:
            exec:
              command:
                - pgrep
                - "-f"
                - "shot_scraper_api.worker"
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 30
            timeoutSeconds: 5
          resources:
            requests:
              cpu: "100m"
              memory: "300Mi"
            limits:
              cpu: "1000m"
              memory: "4Gi"
          env:
            - name: ENV
              value: "prod"
            - name: CACHE_DIR
              value: "/cache"
            - name: QUEUE_BACKEND
              value: "redis"
            - name: QUEUE_PROCESSOR_ENABLED
              value: "true"
            - name: QUEUE_PROCESSOR_CONCURRENCY
              value: "4"
            - name: RENDER_CONCURRENCY
              value: "4"
            - name: POSTPROCESS_CONCURRENCY
              value: "2"
            - name: WORKER_HEARTBEAT_INTERVAL_SECONDS
              value: "10"
            - name: WORKER_HEARTBEAT_TIMEOUT_SECONDS
              value: "45"
            - name: WORKER_STALLED_QUEUE_THRESHOLD_SECONDS
              value: "180"
            - name: REDIS_URL
              value: redis://shots-redis.shot.svc.cluster.local:6379/0
            - name: AWS_ENDPOINT_URL
              value: http://minio.minio.svc.cluster.local:9000
          envFrom:
            - secretRef:
                name: shot-minio-secret
            - secretRef:
                name: shot-secret
          volumeMounts:
            - mountPath: /cache
              name: cache
      imagePullSecrets:
        - name: cluster-regcred
      restartPolicy: Always
      volumes:
        - name: cache
          persistentVolumeClaim:
            claimName: cache
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shot-wayl-one
  namespace: shot
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: shots.wayl.one
      http:
        paths:
          - backend:
              service:
                name: shot-wayl-one
                port:
                  number: 5000
            path: /
            pathType: Prefix
    - host: shots.waylonwalker.com
      http:
        paths:
          - backend:
              service:
                name: shot-wayl-one
                port:
                  number: 5000
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - shots.wayl.one
        - shots.waylonwalker.com
      secretName: shots-cert-tls
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: shot-wayl-one-cleanup
  namespace: shot
spec:
  schedule: "*/15 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: cleanup
              image: curlimages/curl:8.10.1
              args:
                - /bin/sh
                - -lc
                - >-
                  curl --fail --show-error --silent
                  -X POST
                  "http://shot-wayl-one.shot.svc.cluster.local:5000/queue/cleanup?completed_age_hours=24&stale_age_minutes=30"


# ---
# apiVersion: autoscaling/v2
# kind: HorizontalPodAutoscaler
# metadata:
#   name: shot-wayl-one-hpa
#   namespace: shot
# spec:
#   scaleTargetRef:
#     apiVersion: apps/v1
#     kind: Deployment
#     name: shot-wayl-one
#   minReplicas: 3
#   maxReplicas: 6
#   metrics:
#     - type: Resource
#       resource:
#         name: cpu
#         target:
#           type: Utilization
#           averageUtilization: 50
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cache
  namespace: shot
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
  volumeName: pv-hostpath-general-cache
  storageClassName: hostpath
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-hostpath-general-cache
spec:
  capacity:
    storage: 500Mi
  accessModes:
    - ReadWriteMany # Ensure your use case allows this, as hostPath is node-specific
  persistentVolumeReclaimPolicy: Retain
  storageClassName: hostpath
  hostPath:
    path: /mnt/vault/nfs/general/pv/shots/cache
    type: DirectoryOrCreate
